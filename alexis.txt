class taco:
    def __init__(self, tipoDeTaco, tiempoDeEjecucion,tiempoAsignado, cantidad, aguacate, salsa, frijoles, vegetariano):
        assert type(tipoDeTaco) is String
        assert type(tiempoDeEjecucion) is int or float
        assert type(tiempoTotal) is int or float
        assert type(cantidad) is int
        assert type(aguacate) is boolean
        assert type(salsa) is boolean
        assert type(frijoles) is boolean
        assert type(vegetariano) is boolean
        self.tipoDeTaco = tipoDeTaco
        self.tiempoDeEjecucion=tiempoDeEjecucion
        self.tiempoTotal = self.tiempoTotal
        self.cantidad = cantidad
        self.aguacate = aguacate
        self.salsa=salsa
        self.frijoles=frijoles
        self.vegetariano=vegetariano

class orden:
    def __init__(self, sumaTacos,sumaAguas,tiempoDeEjecucion,tiempoTotal):
        assert type(tiempoDeEjecucion) is int or float
        assert type(tiempoTotal) is int or float
        assert type(sumaTacos) is int
        assert type(sumaAguas) is int
        self.sumaTacos = sumaTacos
        self.sumaAguas = sumaAguas
        self.tiempoDeEjecucion = tiempoDeEjecucion
        self.tiempoTotal = tiempoTotal

